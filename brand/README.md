# ZIFT — brand

The mark for this account. One face, every surface.

> **kanıtlanmamış iddiayı denetler** · *evidence over adjectives*

---

## Source of truth

`brand/zift.svg` is the only hand-authored file here. **Every PNG in this
folder is generated by CI** ([`brand-assets.yml`](../.github/workflows/brand-assets.yml))
and must never be edited by hand — the next run will overwrite it.

To change the mark: edit the SVG, push, done. Eighteen assets regenerate.

The vector was traced from the approved raster and the trace was measured,
not eyeballed:

| Check | Value |
|---|---|
| Silhouette fidelity vs. source mask (IoU) | **0.9909** |
| Accent fidelity vs. source mask (IoU) | **0.8967** |
| Vector master size | **4.3 KB** |

## Colours

| Role | Hex | Use |
|---|---|---|
| Zift black | `#0B0B0B` | The mask itself. Never lightened. |
| Signal red | `#FF4D4F` | Eye slits and rim light. The only accent. |
| Surface dark | `#0D1117` | Backgrounds, matches GitHub dark. |
| Off-white | `#F5F2EA` | Wordmark, body text on dark. |

**One accent, not two.** Signal red replaced amber `#F2CC60`, which had
itself replaced cyan `#00E5FF`. A second accent colour is a second brand.

### Why this exact red

The accent changes the geometry not at all — only the fill token. But the
shade was chosen against measured contrast on `#0B0B0B`, not by eye:

| Candidate | Contrast vs. `#0B0B0B` | Verdict |
|---|---|---|
| `#F2CC60` amber | 12.73 | The colour being replaced. |
| `#FB040C` raw source red | 4.80 | Rejected. Sits on the sRGB gamut edge, clips on wide-gamut displays, and only scrapes AA. |
| **`#FF4D4F`** | **6.02** | **Chosen.** Clears AA for text at any size, still unmistakably red. |
| `#FF6B6B` | 7.09 | Reaches AAA but drifts to coral and softens the mask. |

**The trade-off is deliberate and permanent: no red reaches 12.73.** Every
red that climbs toward amber's contrast turns pink first. Roughly half the
old headroom was spent on purpose. Do not "fix" it later by lightening the
accent — that argument was already had and settled.

The legibility gate below is hue-agnostic by construction (it tests
`red > 120` and `red − blue > 45`), so it needed no adjustment: signal red
scores 255 and 176 against those thresholds.

## Type

Monospace, heavy weight, wide tracking. DejaVu Sans Mono in generated
assets; JetBrains Mono where a webfont is already loaded. The wordmark is
always `ZIFT` — uppercase, never letterspaced by hand, never set in a
display or script face.

## Legibility floor

The slits are the whole identity, so CI enforces them. Every generated
icon is re-measured for accent coverage, and the build **fails** if the
accent drops below 1% at 32px or larger.

Measured on the current master:

| Size | Reads as |
|---|---|
| 512 / 180 / 128 px | Full mark, drips visible |
| 48 / 32 px | Head and slits — still unmistakable |
| 16 px | Silhouette only — acceptable, not preferred |

**Minimum size: 32px.** Below that, use the silhouette knowingly.

## Clear space

Keep free space equal to the height of one eye slit on all four sides.
Nothing sits inside that margin — no text, no border, no badge.

## Do not

- Stretch, skew, or rotate the mark.
- Recolour it. Signal red is not orange, crimson, or coral.
- Add a drop shadow, glow, gradient, or outline.
- Place it on a mid-tone background — it needs `#0B0B0B`, `#0D1117`, or `#F5F2EA`.
- Give it a mouth, teeth, a tongue, pupils, or a chest emblem.
- Dress it. A suited silhouette reads as an existing character and is out of bounds.
- Hand-export a PNG. Push the SVG and let CI render it.

## Assets

| File | Where it goes |
|---|---|
| `zift.svg` | Vector master — print, large format, anywhere scalable |
| `zift-mark.svg` | Icon crop — generated |
| `avatar-512.png` / `avatar-128.png` | GitHub, X, LinkedIn profile pictures |
| `banner-1500x500.png` | X header, GitHub org cover, profile README |
| `og-1200x630.png` | `og:image` for link previews |
| `favicon.ico` | Site tab icon (16/32/48 bundled), also copied to repo root |
| `apple-touch-icon.png` | iOS home screen (180px) |
| `zift-mark-192.png` | Android / PWA manifest |

Reference them from anywhere by absolute URL:

```
https://umutseve4.github.io/brand/<file>
```

## Site head

The workflow also owns the icon and link-preview tags in `index.html`.
They live between two markers and are rewritten on every run:

```html
<!-- zift:brand:begin - generated by brand-assets.yml -->
...
<!-- zift:brand:end -->
```

Do not edit inside that block; edit the workflow step instead. Anything
outside the markers is left untouched.

Two caveats, both learned the hard way:

1. The generator commits with the CI-skip token to avoid recursion, so the
   commit that writes the head block does not itself run the site test
   suite. A later ordinary push does.
2. GitHub matches that token anywhere in the commit message, **body
   included**. A commit that merely quotes it will also be skipped — so
   describe it in words, never paste it, when you want the push to build.

## Licence

The mark is an original design. It is not derived from, and must not be
made to resemble, any existing franchise character.

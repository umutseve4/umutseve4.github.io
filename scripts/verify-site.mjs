import fs from 'node:fs';
const html=fs.readFileSync('index.html','utf8'); const proof=fs.readFileSync('proof/index.html','utf8');
const ids=[...html.matchAll(/\sid="([^"]+)"/g)].map(m=>m[1]);
const anchors=[...html.matchAll(/href="#([^"]+)"/g)].map(m=>m[1]);
const contacts=['mailto:2404005065@ogrenci.sbu.edu.tr','https://www.linkedin.com/in/umut-sever-7851b73a6/','https://github.com/umutseve4'];
const blankLinks=[...html.matchAll(/<a\b[^>]*target="_blank"[^>]*>/g)].map(m=>m[0]);
const linkedResume=/<a\b[^>]*(?:resume|résumé|cv)[^>]*>|<a\b[^>]*>[^<]*(?:resume|résumé|cv)[^<]*<\/a>/i.test(html);
const projectCount=(html.match(/<article class="project(?:\s[^"]*)?"/g)||[]).length;
const checks={doctype:/<!doctype html>/i.test(html),lang:/<html lang="en">/.test(html),main:/<main id="main">/.test(html),skip:/class="skip" href="#main"/.test(html),canonical:/rel="canonical" href="https:\/\/umutseve4.github.io\/"/.test(html),jsonld:/application\/ld\+json/.test(html),mobile:/@media\(max-width:760px\)/.test(html),reduced:/prefers-reduced-motion:reduce/.test(html),timezone:/timeZone:'Europe\/Istanbul'/.test(html),noopener:blankLinks.every(link=>/rel="[^"]*noopener[^"]*noreferrer[^"]*"/.test(link)),uniqueIds:ids.length===new Set(ids).size,validAnchors:anchors.every(a=>ids.includes(a)),noMutableRaw:!proof.includes('/main/docs/figures'),fullShas:(proof.match(/[a-f0-9]{40}/g)||[]).length>=11,contacts:contacts.every(url=>html.includes(`href="${url}"`)),fourProjects:projectCount===4,noExternalRuntime:!/@import\s+url|cdnjs|unpkg|jsdelivr|fonts\.googleapis/.test(html),progressiveContent:!/<(?:article|h1)[^>]*(?:hidden|opacity:\s*0)/.test(html),motionLifecycle:/visibilitychange/.test(html)&&/IntersectionObserver/.test(html)&&/cancelAnimationFrame/.test(html),motionObservability:/motionInitialized/.test(html)&&/rafActive/.test(html)&&/rafStarts/.test(html)&&/revealed/.test(html)&&/tiltActive/.test(html),recruiterPositioning:html.includes('Economics Engineering Student → Data Engineering')&&html.includes('Open to data engineering internships and junior opportunities'),ownership:(html.match(/<b>My ownership:<\/b>/g)||[]).length===4,decisions:(html.match(/<b>Key decision:<\/b>/g)||[]).length===4,coldStart:(html.match(/30–60 seconds to wake/g)||[]).length===2,productionBoundary:html.includes('Tested, not production-ready'),noFakeResume:!linkedResume};
for(const [k,v] of Object.entries(checks)) console.log(`${v?'PASS':'FAIL'} ${k}`); if(Object.values(checks).some(v=>!v)) process.exit(1);

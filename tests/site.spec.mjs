import {test,expect} from '@playwright/test';
const contacts=['mailto:umut.sever16@gmail.com','https://www.linkedin.com/in/umut-sever-7851b73a6/','https://github.com/umutseve4','https://x.com/umutseve4','https://www.reddit.com/user/Umutseve4/'];
test('core UX is visible and overflow-free',async({page})=>{const errors=[];page.on('pageerror',error=>errors.push(error.message));await page.goto('/');await expect(page.locator('h1')).toContainText('Build.');await expect(page.locator('nav')).toBeVisible();await expect(page.locator('#work article')).toHaveCount(4);expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false);expect(errors).toEqual([])});
test('keyboard skip link works',async({page})=>{await page.goto('/');await page.keyboard.press('Tab');await expect(page.locator('.skip')).toBeFocused();await page.keyboard.press('Enter');expect(await page.evaluate(()=>location.hash)).toBe('#main')});
test('contact targets are exact and safe',async({page})=>{await page.goto('/');for(const href of contacts)await expect(page.locator(`a[href="${href}"]`)).toHaveCount(1);const unsafe=await page.locator('a[target="_blank"]').evaluateAll(nodes=>nodes.filter(node=>!node.relList.contains('noopener')||!node.relList.contains('noreferrer')).length);expect(unsafe).toBe(0)});
test('JavaScript-off fallback keeps core content visible',async({browser,baseURL})=>{const context=await browser.newContext({javaScriptEnabled:false});const page=await context.newPage();await page.goto(baseURL);await expect(page.locator('h1')).toBeVisible();await expect(page.locator('#work article')).toHaveCount(4);for(const article of await page.locator('#work article').all())await expect(article).toBeVisible();await context.close()});
test('normal motion initializes with one running RAF',async({page})=>{await page.goto('/');await expect.poll(()=>page.evaluate(()=>document.documentElement.dataset.motionInitialized)).toBe('true');await expect.poll(()=>page.evaluate(()=>document.documentElement.dataset.rafActive)).toBe('true');expect(Number(await page.evaluate(()=>document.documentElement.dataset.rafStarts))).toBe(1)});
test('fine-pointer card tilt is bounded and resets',async({page,browserName})=>{test.skip(browserName!=='chromium');await page.goto('/');const card=page.locator('.project').first();await card.hover({position:{x:20,y:20}});await expect.poll(()=>card.getAttribute('data-tilt-active')).toBe('true');const values=await card.evaluate(node=>[parseFloat(getComputedStyle(node).getPropertyValue('--tilt-x')),parseFloat(getComputedStyle(node).getPropertyValue('--tilt-y'))]);expect(values.every(value=>Math.abs(value)<=2.01)).toBe(true);await page.mouse.move(0,0);await expect.poll(()=>card.getAttribute('data-tilt-active')).toBe('false')});
test('reduced motion suppresses canvas and RAF',async({page})=>{await page.emulateMedia({reducedMotion:'reduce'});await page.goto('/');await expect(page.locator('#field')).toHaveCSS('display','none');await expect.poll(()=>page.evaluate(()=>document.documentElement.dataset.rafActive)).toBe('false');await expect.poll(()=>page.evaluate(()=>document.documentElement.dataset.motionState)).toBe('reduced')});
test('offscreen hero pauses and resumes without duplicate loop',async({page})=>{await page.goto('/');const initial=Number(await page.evaluate(()=>document.documentElement.dataset.rafStarts));await page.locator('#contact').scrollIntoViewIfNeeded();await expect.poll(()=>page.evaluate(()=>document.documentElement.dataset.rafActive)).toBe('false');await page.locator('.hero').scrollIntoViewIfNeeded();await expect.poll(()=>page.evaluate(()=>document.documentElement.dataset.rafActive)).toBe('true');const resumed=Number(await page.evaluate(()=>document.documentElement.dataset.rafStarts));expect(resumed).toBe(initial+1);await page.waitForTimeout(150);expect(Number(await page.evaluate(()=>document.documentElement.dataset.rafStarts))).toBe(resumed)});
test('proof uses immutable source URLs',async({page})=>{await page.goto('/proof/');const urls=await page.locator('[data-src]').evaluateAll(nodes=>nodes.map(node=>node.dataset.src));expect(urls.every(url=>/[a-f0-9]{40}/.test(url))).toBe(true)});
